import _ from "lodash";
//import { api } from '../../shared'

const initialState = {};

export default function reducer(state = initialState, action = {}) {
  switch (action.type) {
    default:
      return state;
  }
}
